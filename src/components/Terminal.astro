<main class="terminal">
  <section class="terminal-header">
    <div class="header-section left">
      <section class="window-controls">
        <div class="window-controls-button red"></div>
        <div class="window-controls-button yellow"></div>
        <div class="window-controls-button green"></div>
      </section>

      <span class="terminal-header-title">terminal</span>

      <span class="terminal-header-pwd">~/home/user</span>
    </div>

    <div class="header-section right"></div>
  </section>

  <section class="terminal-content">
    <span class="prompt">[thisguyvims@]</span>
  </section>
</main>

<style>
.terminal {
  align-items: flex-start;
  border-radius: var(--radius-l);
  border: 1px solid var(--color-overlay-0);
  display: flex;
  flex-direction: column;
  grid-column: 2;
  justify-content: flex-start;
  overflow: hidden;
  width: 46rem;
  box-shadow:
    rgba(0, 0, 0, 0.3) 0px 19px 38px,
    rgba(0, 0, 0, 0.22) 0px 15px 12px;
}

.terminal-header {
  align-items: center;
  background-color: var(--color-mantle);
  border-radius: var(--radius-m);
  display: flex;
  flex-direction: row;
  height: 1.5rem;
  justify-content: space-between;
  padding: 0.2rem 0.5rem;
  width: 100%;
  font-weight: 600;
}

.terminal-content {
  padding: 0.2rem 0.5rem;
}

.header-section {
  align-items: center;
  display: flex;
  gap: 0.8rem;
}

.header-section.left {
  justify-content: flex-start;
}

.header-section.right {
  justify-content: flex-end;
}

.terminal-header-title {
  color: var(--color-mauve);
}

.terminal-header-pwd {
  color: var(--color-blue);
}

.window-controls {
  align-items: center;
  display: flex;
  flex-direction: row;
  gap: 0.2rem;
  height: 100%;
  justify-content: center;
}

.window-controls-button {
  border-radius: var(--radius-full);
  border: 1px solid rgba(0, 0, 0, 0.2);
  height: 0.6rem;
  width: 0.6rem;
}

.window-controls-button.red {
  background-color: var(--color-red);
}

.window-controls-button.yellow {
  background-color: var(--color-yellow);
}

.window-controls-button.green {
  background-color: var(--color-green);
}
</style>
